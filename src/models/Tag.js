import axios from 'axios'
import {myConfig} from '../config'

class ImagesModel {

  // static postTagRating(user_id, tag, status) {
  //   let request = axios.post(myConfig.api_url + "api/tags/ratings", {user_id: user_id, tag: tag, status: status});
  //   return request
  // }
}

export default ImagesModel
